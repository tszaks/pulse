# Postgres (local dev)
DB_PASSWORD=changeme_local

# Umami
APP_SECRET=changeme_run_openssl_rand_hex_32
UMAMI_USERNAME=admin
UMAMI_PASSWORD=changeme_after_first_login

# Pulse Agent API
# Generate with: openssl rand -hex 32
PULSE_API_KEYS=your_secret_key_here

# Only needed when running agent-api standalone (not via docker-compose)
# docker-compose sets this automatically to http://umami:3000
UMAMI_BASE_URL=http://localhost:3000
