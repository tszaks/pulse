# Copy to .env and fill in values
# Generate APP_SECRET with: openssl rand -hex 32

DB_PASSWORD=changeme_local
APP_SECRET=changeme_run_openssl_rand_hex_32

# Set after Umami is running (from Railway or local)
UMAMI_BASE_URL=http://localhost:3000
UMAMI_API_KEY=
